rootProject.name = 'takin-cloud'
// 包含的模块
include(':constant')
include(':model')
include(':sdk')
include(':app')

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("./libs.versions.toml"))
        }
    }
}
